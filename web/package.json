{
  "name": "scanfit-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit",
    "deploy": "npm run build && wrangler pages deploy dist --project-name scanfit"
  },
  "dependencies": {
    "onnxruntime-web": "^1.27.0",
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^6.2.108",
    "@techstark/opencv-js": "^5.0.0-release.1"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "^8.1.5",
    "wrangler": "^4.115.0"
  }
}
